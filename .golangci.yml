issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
        - dupl
linters:
  disable-all: true
  enable:
    - errcheck
    - govet
    - staticcheck
    - ineffassign
    - revive
    - gocritic
    - bodyclose
    - gosimple
    - unused
